services:
  hugo:
    image: hugomods/hugo:0.132.2
    command: hugo server --bind 0.0.0.0
    ports:
      - "1313:1313"
    volumes:
      - .:/src

# docker run --rm -it -p 1313:1313 ` -v ${PWD}:/src ` hugomods/hugo:0.132.2 ` hugo server --bind 0.0.0.0
# docker compose up